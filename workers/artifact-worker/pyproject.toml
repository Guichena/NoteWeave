[project]
name = "noteweave-artifact-worker"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "pydantic-settings>=2.6.0",
  "pytest>=9.0.0",
  "uvicorn>=0.32.0"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
